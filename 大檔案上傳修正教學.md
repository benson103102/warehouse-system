# 修正「檔案太大匯入失敗（HTTP 413）」

## 為什麼會這樣

Cloud Run 前面的反向代理對**單一 HTTP 請求**有寫死的 **32MB 硬上限**，跟你設定多少
記憶體、CPU 都沒關係，也沒有開關可以調高。你的 xlsx 檔案約 90MB，遠遠超過，所以
瀏覽器把檔案塞進上傳請求那一刻，還沒送到我們的程式碼，就先被 Cloud Run 擋下來回
413 了。

## 怎麼修的

改成「先讓瀏覽器把檔案直接傳去 Google Cloud Storage（沒有 32MB 限制），後端再從
Cloud Storage 把檔案讀進來處理」，繞過這道關卡。這次改了兩個檔案：

- `backend/app/routers/ingest.py`：新增兩支 API（`/upload_url` 拿直傳網址、
  `/upload_from_gcs` 通知後端去讀），原本的 `/upload`（小檔案用）完全沒動。
- `frontend/index.html`：上傳按鈕現在會自動判斷——**檔案超過 28MB 才會走新的直傳
  流程**，28MB 以下還是走原本的方式，兩條路並存。
- `backend/requirements.txt`：新增一個套件 `google-cloud-storage`。

你不用手動選要走哪條路、也不用改匯入頁面的操作方式，選檔案、按「上傳並匯入」就好，
系統會自己判斷。

---

## 你需要在 Google Cloud 那邊多做的設定（只需要做一次）

回到 Cloud Shell（跟之前部署時同一個地方），依序打以下指令。

### 1. 建立一個專門放「暫存上傳檔」的儲存桶

```bash
gcloud storage buckets create gs://warehouse-system-505315-uploads \
  --location=asia-east1 \
  --uniform-bucket-level-access
```

### 2. 讓 Cloud Run 的服務帳戶可以讀寫這個桶

```bash
gcloud storage buckets add-iam-policy-binding gs://warehouse-system-505315-uploads \
  --member="serviceAccount:813095872171-compute@developer.gserviceaccount.com" \
  --role="roles/storage.objectAdmin"
```

### 3. 啟用「產生直傳網址」需要的 API，並允許服務帳戶簽自己的名

Cloud Run 上沒有本機金鑰檔可以簽章，要透過 IAM 這個 API 代簽，服務帳戶需要對「自己」
有權限：

```bash
gcloud services enable iamcredentials.googleapis.com

gcloud iam service-accounts add-iam-policy-binding \
  813095872171-compute@developer.gserviceaccount.com \
  --member="serviceAccount:813095872171-compute@developer.gserviceaccount.com" \
  --role="roles/iam.serviceAccountTokenCreator"
```

### 4. 設定這個桶允許瀏覽器直接上傳（CORS）

先建立一個設定檔（把 `你的網址` 換成你實際的 Cloud Run 網址，就是
`https://warehouse-system-813095872171.asia-east1.run.app` 這個）：

```bash
cat > /tmp/cors.json <<'EOF'
[
  {
    "origin": ["https://warehouse-system-813095872171.asia-east1.run.app"],
    "method": ["PUT"],
    "responseHeader": ["Content-Type"],
    "maxAgeSeconds": 3600
  }
]
EOF

gcloud storage buckets update gs://warehouse-system-505315-uploads --cors-file=/tmp/cors.json
```

### 5.（建議順手做）設定暫存檔自動清理，避免累積佔空間

我們的程式碼上傳處理完會自動刪暫存檔，但保險起見加一條規則：超過 1 天沒刪掉的
暫存檔，系統自動幫你清掉：

```bash
cat > /tmp/lifecycle.json <<'EOF'
{
  "rule": [
    {"action": {"type": "Delete"}, "condition": {"age": 1, "matchesPrefix": ["uploads-staging/"]}}
  ]
}
EOF

gcloud storage buckets update gs://warehouse-system-505315-uploads --lifecycle-file=/tmp/lifecycle.json
```

---

## 更新程式碼並重新部署

1. **先在你電腦上把這次改好的檔案存回專案資料夾**（我已經幫你存回去了，見對話裡的
   檔案）。
2. **推送到 GitHub**（照「推送到GitHub教學.md」的方法：Git Bash 裡 `git add .` →
   `git commit -m "新增大檔案直傳"` → `git push`）。
3. 回到 **Cloud Shell**，把最新程式碼拉下來：

   ```bash
   cd ~/warehouse-system
   git pull
   ```

4. 重新部署，**這次要多帶一個環境變數** `WAREHOUSE_UPLOAD_BUCKET`（指向剛剛建立的
   儲存桶）：

   ```bash
   gcloud run deploy warehouse-system \
     --source . \
     --region asia-east1 \
     --allow-unauthenticated \
     --set-env-vars WAREHOUSE_DATA_DIR=/data,WAREHOUSE_UPLOAD_BUCKET=warehouse-system-505315-uploads \
     --max-instances 1 \
     --memory 1Gi
   ```

   > ⚠️ `--set-env-vars` 每次都是「整組覆蓋」，不是新增，所以要把之前設過的
   > `WAREHOUSE_DATA_DIR` 也一起寫進去，不然會不見（上面這行已經幫你寫好兩個，照抄
   > 貼上即可）。

5. 部署完成後，回到網站頁面重新整理，再上傳一次那個 90MB 的檔案試試看。這次應該會
   看到訊息變成「檔案較大，改用直傳雲端硬碟方式上傳中…」，上傳完換成「已上傳到雲端
   暫存，通知後端開始解析…」，然後進到原本的「背景解析中…已等待 N 秒」畫面（90MB、
   百萬列等級的資料，解析可能要好幾分鐘，屬正常現象，可以先切去別的頁面等等再回來）。

---

## 如果還是失敗

- 如果卡在「無法取得直傳網址」：多半是 Part 2、3 的權限還沒生效，等 2～3 分鐘再試。
- 如果卡在「直傳雲端硬碟失敗」：先按 F12 打開瀏覽器開發者工具、切到 Network 分頁，
  找那個 PUT 請求，看錯誤訊息（常見是 CORS 設定的網址跟你實際網址不一致，回頭檢查
  Part 4 `cors.json` 裡的網址是否跟網址列一模一樣，包含 `https://` 開頭）。
- 把完整錯誤訊息／截圖貼給我，我可以繼續幫你排查。
