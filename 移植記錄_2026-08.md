# 第二批移植記錄（2026-08-12）

## 背景

比對 HTML 原型（`儲位配置與揀貨策略優化系統 1.html`，約200個運算函式）與
`warehouse-system舊版/backend/app/services/analytics.py` 後，發現 analytics.py
先前只移植了「SKU/類別ABC分級、共同揀取、需求預測、簡易批次改善率、儲位分配、KPI」
六大類的基礎版本。以下三塊原型有、但後端完全沒有的邏輯，本次已補上：

1. **地理分群＋播種式批次模擬**（依出貨日期×配送區域分波、單日揀貨計畫）
2. **精細版儲位配置**（依「共同揀取關聯」把商品分群、群內相鄰擺放，取代原本
   只把儲位「分給類別」的大鍋分配）
3. **預測式 ABC 分級**（用預測值而非歷史值分級）

## 改了哪些檔案

- `backend/app/services/analytics.py`：新增第 8～11 節（材質分類、預測式ABC、
  精細版儲位配置、地理分群批次），檔頭對照表已同步更新。
- `backend/app/routers/abc.py`：新增 `GET /api/abc/predicted_category`、
  `GET /api/abc/predicted_sku`。
- `backend/app/routers/storage.py`：新增 `GET /api/storage/wh_assignment`
  （可用 `sku_id` 查單一商品目前被分到哪個分區）。
- `backend/app/routers/batch.py`：新增 `GET /api/batch/strategies`（逐單vs批次
  比較＋地理分佈Top15）、`GET /api/batch/dates`（有出貨紀錄的日期清單）、
  `GET /api/batch/day_plan`（單日揀貨班表）。原本的 `/simulate` 維持不動。
- `backend/app/jsonsafe.py`：補上「字典的 key 也要轉型別」（新增的端點會回傳
  「SKU/類別ID → ...」這種以 numpy 整數當 key 的字典，先前 `clean()` 只轉換了
  value，沒轉換 key，標準 json 模組遇到 `numpy.int64` 當 key 會直接噴錯）。

## 一項刻意的簡化（跟原型不同，但是刻意的，不是漏做）

原型的地理分群批次（`batchParseGeo`）自己內建了一份「台灣縣市鄉鎮升格對照表」
去把地址正規化成縣市＋行政區。但實際上 `cleaning_core.py` 的清洗流程（Part 18／
21／22／23）已經做了**更完整**的地址正規化（含地址前綴回推、簡寫展開、稀疏地址
排除等原型沒有的邏輯），清洗完的乾淨資料已經有現成的「配送區域」欄位可以直接用。
所以這次沒有把原型那份地址對照表整個搬過來，只寫了 `_split_city_district()`
把「配送區域」字串拆回（縣市, 行政區）兩段給批次分波用。

## 已驗證方式

開發環境連不上 PyPI，無法直接起 FastAPI 伺服器做端對端測試（跟 README 記載的
既有限制一樣）。應變做法：

1. 用合成資料（分別造了 2,500 列／124,795 列兩組，含多縣市地址、多商品類別、
   跨月出貨日期）直接呼叫 `analytics.py` 新增的每一個函式，人工核對輸出的資料
   結構、數值合理性（改善率介於 0～100%、ABC 分級加總比例、地理分佈排序等）。
2. 效能測試：12.4萬列（2.8萬張訂單）情境下，`compute_wh_assignment`（含預測式
   ABC＋共同揀取分析）約 2.9 秒、`build_batch_orders` 優化後約 0.8 秒（原本用
   `iterrows()` 要 6.4 秒，已改用 `itertuples()`）——這支是這次新增邏輯裡跟「大
   型資料檔」最相關的效能熱點，已預先處理。
3. 邊界情況：空 DataFrame、資料不足以預測（月數<7）、查無此SKU、查無此日期
   出貨紀錄，皆有對應的 422／404 回應，不會整支掛掉。
4. 所有異動檔案跑過 `ast.parse()` 語法檢查通過。

**沒做到的驗證**：無法實際啟動 FastAPI 伺服器打 API、也沒有你們的真實資料可以
核對數字是否符合業務直覺（原本的移植同樣有這個限制，見 README「這份程式碼是
怎麼驗證過的」一節）。建議你在自己電腦上 `uvicorn app.main:app --reload` 跑起來、
用 Swagger（`/docs`）打新增的 5 支端點，拿真實資料實際核對一次數字。

## 新增端點速覽

| 端點 | 說明 |
|---|---|
| `GET /api/abc/predicted_category` | 預測式類別 ABC（貨架依預測揀貨次數、棧板依預測出貨量） |
| `GET /api/abc/predicted_sku` | 預測式 SKU ABC（同上，SKU 層級，`top_n` 控制回傳筆數） |
| `GET /api/storage/wh_assignment` | 精細版儲位配置（共同揀取關聯聚簇擺放），`sku_id` 可查單一商品 |
| `GET /api/batch/strategies` | 地理分群＋播種式批次：逐單 vs 批次回合數/距離改善率、地理Top15 |
| `GET /api/batch/dates` | 有出貨紀錄的日期清單 |
| `GET /api/batch/day_plan?day=YYYY-MM-DD` | 單日揀貨班表（分波、回合、SKU揀取順序） |

## 建議的後續（沒做的部分）

- 前端 `frontend/index.html` 目前只呼叫既有端點，還沒接上面 5 支新端點的畫面
  （原型對應畫面：儲位配置頁「改善後地圖」分頁、批次併單頁「地理分群」與「單日
  查詢」區塊、ABC頁「預測式分級」分頁）。這次範圍是後端運算邏輯，前端畫面串接
  建議另外開一輪。
- `build_batch_orders` 對「線上百萬列」等級資料仍是 Python 迴圈組裝 dict（雖已
  用 itertuples 大幅加速），若之後實測發現仍是瓶頸，可以再進一步改成完全向量化
  （用 groupby+transform 取代逐列組 dict），目前先以「先求對、效能夠用」為原則。
