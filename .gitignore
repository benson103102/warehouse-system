# === 開發工具/編輯器設定（本機用，別人不需要看到）===
.vscode/

# === Python 虛擬環境（跑一次 pip install 就能重建，不用進版控）===
backend/.venv/
**/__pycache__/
*.pyc

# === 執行期資料（使用者帳號、上傳的檔案、清洗結果快取、SQLite 資料庫）===
# 這些是「你的系統跑起來之後」自己產生的資料，不是程式碼本身，
# 通常也包含真實業務資料，不應該出現在公開/共用的 repo 裡。
backend/data/

# === 作業系統雜項檔 ===
.DS_Store
Thumbs.db
desktop.ini

# === 大型原始資料檔（真實業務資料，不應該進版控）===
P零售物流data.xlsx
